import React from 'react';
import { storiesOf } from '@storybook/react';
import RoomControls from './RoomControls';

storiesOf('atom/RoomControls', module).add('Default', () => (
  <RoomControls onClick={() => {}} />
));
